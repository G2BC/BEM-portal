services:
  web:
    image: ${APP_IMAGE:-ghcr.io/g2bc/bem-portal}:${IMAGE_TAG:-latest}
    container_name: bem-portal
    restart: unless-stopped
    ports:
      - "4002:80"
    security_opt:
      - no-new-privileges:true
